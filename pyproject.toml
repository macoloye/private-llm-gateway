[project]
name = "private-inference-gateway"
version = "0.1.0"
description = "Privacy and security gateway for self-hosted LLM inference backends."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"

[project.scripts]
private-inference-gateway = "gateway.__main__:main"
